query Launches {
  launchesPast(limit: 20, order: "desc", sort: "launch_date_utc") {
    launch_date_utc
    mission_name
    id
    links {
      mission_patch_small
    }
    rocket {
      rocket_name
    }
  }
}
